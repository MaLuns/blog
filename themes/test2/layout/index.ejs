<%# 首页 %>
<%- partial('./_partial/header') %>
<div id="dplayer"></div>
<main class="container">
    <section id="posts" class="posts-expand">
        <%- partial('./_partial/archive', {pagination: 2, index: true}) %>
    </section>
    <% if(page.prev!=0 || page.next !=0){%>

    <div id="ddddsss" onclick="loadmore()" style="text-align: center">asdasdasd</div>

    <% } %>
    <script>
        var INDEX_CONFIG = {
            PAGE_TOTAL:<%- page.total %>,
        }
        function loadmore() {
            $.ajax({
                url: "page/2",
                success: res => {
                    let a = document.createElement("div");
                    a.innerHTML = res;
                    document.getElementsByClassName("posts-expand")[0].append(a.getElementsByClassName("posts-expand")[0])
                }
            })
        }
    </script>
</main>
<!-- index.ejs -->