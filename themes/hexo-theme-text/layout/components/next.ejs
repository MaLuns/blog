<% if(post.prev || post.next){ %>
    <div class="prev-next-content">
        <div class="btn-prev">
            <% if(post.prev){ %>
                <div>
                   <i class="iconfont cg-shangyige"></i> 
                   <%- __('post.last_post') %>
                </div>
                <a class="_pjax" href="<%- url_for(post.prev.path) %>"><%- post.prev.title %></a>
            <%}%>
        </div>
        <div class="btn-next">
            <% if(post.next){ %>
                <div>
                    <%- __('post.next_post') %>
                    <i class="iconfont cg-xiayige"></i>
                </div>
                <a class="_pjax" href="<%- url_for(post.next.path) %>"><%- post.next.title %></a>
            <%}%>
        </div>
    </div>
<% } %>